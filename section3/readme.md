# 🗄️ Session 3: Database & Integration

**เวลา:** 13:00 - 14:00 | Connecting the Data Layer

---

## 🛠️ สิ่งที่ต้องเตรียม (Prerequisites)

ก่อนเริ่ม Session 3 ในฐานะที่เราจะเรียนรู้การเชื่อมต่อฐานข้อมูล ผู้เรียนควรสมัครบัญชีและเตรียมเครื่องมือดังนี้:

1. **NeonDB Account (ฐานข้อมูล PostgreSQL ฟรีบนคลาวด์)**
   - 🔗 [สมัครใช้งาน NeonDB](https://neon.tech/) (ใช้บัญชี GitHub หรือ Google สมัครได้เลย)
   - _(ทางเลือกสำหรับสาย Docker)_ เราได้เตรียมไฟล์ `docker-compose.yml` สำหรับรัน PostgreSQL ในเครื่องไว้ให้ศึกษาอยู่ในโฟลเดอร์ `code/postgres`
2. **VS Code Extension (ทางเลือกแต่แนะนำ)**
   - `Prisma` (ช่วยทำสีและ Auto-complete โค้ดของไฟล์ `.prisma`)
   - `Docker` (กรณีใช้ PostgreSQL ผ่าน Docker)

📌 **หมายเหตุโค้ดตัวอย่าง:** ในโฟลเดอร์ `code` ของ Session นี้ จะมี 2 แบบให้ดูเป็นตัวอย่าง:

- `code/postgres` : ใช้สำหรับรัน Docker PostgreSQL ในเครื่อง
- `code/neondb` : ใช้สำหรับต่อขึ้น Cloud อย่าง NeonDB
  โครงสร้างโค้ดเหมือนกัน **ต่างกันแค่ `DATABASE_URL` ในไฟล์ `.env` เท่านั้น**

---

## 💻 Commands สำหรับติดตั้ง Prisma (Setup Commands)

เราจะติดตั้งเครื่องมือที่ช่วยให้ Node.js คุยกับฐานข้อมูลได้ง่ายขึ้น นั่นคือ **Prisma ORM**
ให้เปิด Terminal **(ที่อยู่ในโฟลเดอร์ `backend`)** แล้วรันคำสั่งตามลำดับ:

```bash
# 1. ติดตั้ง Prisma CLI เป็นเครื่องมือช่วยในการพัฒนา (Dev Dependency)
npm install prisma --save-dev

# 2. เริ่มต้นใช้งาน Prisma (จะสร้างโฟลเดอร์ prisma/ และไฟล์ .env ขึ้นมา)
npx prisma init
```

เรายังต้องติดตั้งตัวเชื่อมต่อ (Client) ที่เอาไว้ใช้ดึงข้อมูลจริงๆ ในตัวโค้ดด้วย

```bash
# 3. ติดตั้ง Prisma Client
npm install @prisma/client
```

---

## 📖 คำอธิบายคำสั่งใน package.json (Scripts Explanation)

ในไฟล์ `package.json` เราได้ตั้งค่าคำสั่งย่อ (Scripts) เพื่อให้ทำงานกับ Prisma ได้สะดวกขึ้น ดังนี้:

- `npm run prisma:init` : ใช้ครั้งแรกเพื่อสร้างไฟล์พื้นฐาน (`schema.prisma` และ `.env`)
- `npm run prisma:generate` : สั่งให้ Prisma สร้างโค้ด (Client) มาใส่ใน `node_modules` เพื่อให้เราเรียกใช้ใน `index.js` ได้
- `npm run prisma:migrate` : (แนะนำสำหรับ Dev) บันทึกการเปลี่ยนโครงสร้างตารางลงในไฟล์ (Migration) และอัปเดตฐานข้อมูลจริงให้ตรงกัน
- `npm run prisma:deploy` : ใช้รันไฟล์ Migration ที่มีอยู่แล้วเพื่ออัปเดตฐานข้อมูล (มักใช้ตอน Deploy ขึ้น Production)
- `npm run prisma:push` : อัปเดตฐานข้อมูลให้ตรงกับไฟล์ Schema ทันที (เหมาะกับช่วงทดลองแก้แบบเร็วๆ โดยไม่ต้องการเก็บประวัติการแก้)
- `npm run prisma:studio` : เปิดโปรแกรมจัดการฐานข้อมูลแบบเว็บ (หน้าตาสวยงาม ดูขอมูลง่ายเหมือน Excel)
- `npm run prisma:format` : ช่วยจัดรูปแบบโค้ดในไฟล์ `schema.prisma` ให้เป็นระเบียบและอ่านง่ายอัตโนมัติ

---

## 1️⃣ Database & Prisma ORM

### 🟢 Database Concept เบื้องต้น

เราจะใช้ **Relational Database (ฐานข้อมูลเชิงสัมพันธ์)** ซึ่งหลักการจำง่ายๆ คือ **"มันเหมือนไฟล์ Excel!"**

- **Table (ตาราง):** เหมือนชีต 1 แผ่นเก็บข้อมูลเรื่องหนึ่ง (เช่น ตาราง Users, ตาราง Products)
- **Column (คอลัมน์):** หัวข้อข้อมูลในแต่ละแนวตั้ง (เช่น id, name, email)
- **Row (แถว / Record):** ข้อมูลของแต่ละคน/แต่ละชิ้นในแนวนอน 1 บรรทัดแถว

### 🟢 ทำไมเราถึงใช้ PostgreSQL คู่กับ NeonDB?

**PostgreSQL** เป็นฐานข้อมูลฟรีที่ดีและทรงพลังระดับโลก ส่วน **NeonDB** เป็นบริการที่ให้เราเช่าเซิร์ฟเวอร์ PostgreSQL ลอยอยู่บนระบบ Cloud แบบ**ฟรีๆ** ไม่ต้องโหลดโปรแกรม Database (ที่ตั้งค่ายากและหนักเครื่อง) มาลงในคอมตัวเองเลย เหมาะกับผู้เริ่มต้นเอามากๆ!

### 🟢 Prisma ORM คืออะไร?

**ORM (Object-Relational Mapping)** คือ "ล่ามแปลภาษา"
ปกติเวลาจะคุยกับ Database เราต้องเขียนภาษา `SQL` (เช่น `SELECT * FROM users WHERE id=1`) แต่ถ้าใช้ **Prisma** เราสามารถสั่งงานด้วยภาษา JavaScript/TypeScript ธรรมดาที่คุ้นเคยลงไปชิลๆ เช่น `prisma.user.findMany()` แล้ว Prisma จะเอาไปแปลงเป็นคำสั่งเข้า Database ให้เสร็จสรรพ

### 📊 Diagram แสดงการเชื่อมต่อ (Backend ↔ Database)

```mermaid
sequenceDiagram
    participant Express as 🚂 Express (Controller)
    participant Prisma as 🪄 Prisma ORM
    participant Database as 🗄️ NeonDB (PostgreSQL)

    Express->>Prisma: สั่ง prisma.user.findMany()
    Prisma->>Database: 🤖 แปลงเป็น "SELECT * FROM public.User;"
    Database-->>Prisma: ส่งตาราง Row ข้อมูลกลับมา
    Prisma-->>Express: 📦 แพ็กเป็น Array ของ JavaScript Object ให้!
```

### 💻 ตัวอย่าง Code: การสร้าง Prisma Schema

หลังจากรัน `npx prisma init` เราจะได้ไฟล์ชื่อ `prisma/schema.prisma` เอาไว้ใช้ออกแบบตาราง

```prisma
// ไฟล์: prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql" // ระบุว่าเราใช้ PostgreSQL
  url      = env("DATABASE_URL")
}

// 📌 ออกแบบตาราง Users
model User {
  id        Int      @id @default(autoincrement()) // เป็น Primary Key และนับเลข Auto
  name      String
  email     String   @unique                       // ห้ามซ้ำ
  role      String   @default("user")
  createdAt DateTime @default(now())               // บันทึกเวลาที่สมัครอัตโนมัติ
}
```

เมื่อออกแบบเสร็จ จะต้องใช้คำสั่งเพื่อเอาแบบแปลนนี้ไปสร้างตารางจริงๆ บน NeonDB:

```bash
npx prisma db push
```

---

## 2️⃣ Full CRUD Flow & Environment Variables

### 🟢 Environment Variables (`.env`) คืออะไร?

มันคือ "ไฟล์ซ่อนความลับระดับชาติ" 🤫
ข้อมูลบางอย่างที่สำคัญมากๆ เช่น **รหัสผ่านเชื่อม Database `DATABASE_URL`** หรือรหัสผ่านอีเมล เราจะไม่ยอมพิมพ์ตรงๆ ลงไปในโค้ด `index.js` หรือเอาขึ้น GitHub เด็ดขาด (มิฉะนั้นอาจโดนแฮกเอาเซิร์ฟเวอร์ไปขุดบิตคอยน์!)

**วิธีการใช้:**

1. เก็บความลับไว้ในไฟล์ชื่อ `.env`
   ```env
   # ไฟล์: .env (ตั้งอยู่ชั้นนอกสุดของโปรเจกต์)
   DATABASE_URL="postgresql://ชื่อผู้ใช้:รหัสผ่าน@ep-dawn-shape-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require"
   PORT=5000
   ```
2. ดึงความลับไปใช้ (Node.js ใช้ผ่าน `process.env.ชื่อตัวแปร`) ซึ่ง Prisma จะดึงจาก `.env` ให้เองอยู่แล้ว

⚠️ **สำคัญสุดๆ:** ต้องสร้างไฟล์ชื่อ `.gitignore` ไว้บล็อกไม่ให้เอาโค้ด `.env` ส่งเข้า GitHub

```text
# ไฟล์: .gitignore
node_modules
.env
```

### 💻 ตัวอย่าง Code: Full CRUD Flow ใน Express ควบ Prisma

ในบทก่อนหน้าเราเขียนเป็น Array ปลอมๆ (`[ {id: 1, name: "สมชาย"} ]`) บัดนี้เราจะเปลี่ยนมาดึงของจริงจาก Database แล้ว
ขอให้ลบโค้ดเดิมใน `routes/users.js` ทิ้ง แล้วใส่โค้ดใหม่ทั้งหมดดังนี้:

````javascript
const express = require("express");
const router = express.Router();
// ดึงที่งัดแงะ Prisma เข้ามา
const { PrismaClient } = require("@prisma/client");

const prisma = new PrismaClient(); // เปิดใช้งาน


```javascript
const express = require("express");
const cors = require("cors");
// ดึงที่งัดแงะ Prisma เข้ามา
const { PrismaClient } = require("@prisma/client");

// ==============================
// 🟩 C - Create (สร้าง User ใหม่)
// ==============================
router.post("/", async (req, res) => {
  try {
    const { name, email, role } = req.body;

    // สั่งบันทึกลง Database!
    const newUser = await prisma.user.create({
      data: { name, email, role: role || "user" }, // ยัดตัวแปรเข้า Column (ถ้าไม่ได้ส่ง role มา จะเป็น user)
    });

    res.status(201).json({
      message: "สร้างผู้ใช้สำเร็จ!",
      data: newUser
    });
  } catch (error) {
    console.log("Error:", error);
    res.status(500).json({ error: "อีเมลอาจซ้ำ หรือมีบางอย่างผิดพลาด" });
  }
});

// ==============================
// 🟦 R - Read (ดึงข้อมูล Users ทั้งหมด)
// ==============================
router.get("/", async (req, res) => {
  try {
    const users = await prisma.user.findMany(); // สั่งดึงทุกบรรทัดจบ!
    res.status(200).json(users);
  } catch (error) {
    res.status(500).json({ error: "เกิดข้อผิดพลาดในการดึงข้อมูล" });
  }
});

// ==============================
// 🟨 U - Update (อัปเดตข้อมูล User)
// ==============================
router.put("/:id", async (req, res) => {
  try {
    const userId = parseInt(req.params.id);
    const updateData = req.body;

    const updatedUser = await prisma.user.update({
      where: { id: userId }, // ค้นหาคนที่จะแก้
      data: updateData,      // ใส่ข้อมูลใหม่ลงไป
    });

    res.status(200).json({
      message: "แก้ไขข้อมูลสำเร็จ!",
      data: updatedUser
    });
  } catch (error) {
    console.log("Error:", error);
    // Prisma จะโยน error รหัส P2025 ถ้าหา user id ไม่เจอ
    if (error.code === 'P2025') {
      return res.status(404).json({ error: "ไม่พบผู้ใช้ที่ต้องการแก้ไข" });
    }
    res.status(500).json({ error: "เกิดข้อผิดพลาดในการแก้ไขข้อมูล" });
  }
});

// ==============================
// 🟥 D - Delete (ลบข้อมูล User)
// ==============================
router.delete("/:id", async (req, res) => {
  try {
    const userId = parseInt(req.params.id);

    const deletedUser = await prisma.user.delete({
      where: { id: userId }
    });

    res.status(200).json({
      message: "ลบผู้ใช้สำเร็จ!",
      data: deletedUser
    });
  } catch (error) {
    console.log("Error:", error);
    if (error.code === 'P2025') {
      return res.status(404).json({ error: "ไม่พบผู้ใช้ที่ต้องการลบ" });
    }
    res.status(500).json({ error: "เกิดข้อผิดพลาดในการลบข้อมูล" });
  }
});

module.exports = router;


````

_ส่วนของ `index.js` จะยังคงเดิม เพียงแค่เช็คว่ามีการเรียก `app.use("/api/users", userRoutes);` ไว้แค่นั้นพอ_

### 🚨 Common Database Mistakes (ข้อผิดพลาดพบบ่อยที่ทำให้ DB พัง)

> [!WARNING]
>
> - **ลืมสร้าง `.env` หรือเอาโค้ดดันขึ้น GitHub:** เป็นเหตุผลอันดับ 1 ที่ผู้เริ่มต้นจะโดนบอทสูบฐานข้อมูลรหัสผ่าน ท่องไว้ว่าอย่าดัน `.env` ขึ้น Git
> - **ลืมพิมพ์ `async`/`await`:** การคุยกับ Database ต้องใช้เวลา (เน็ตวิ่งไปวิ่งกลับ) ถ้าเราไม่รอ โค้ดจะวิ่งข้ามและพังได้เลยเพราะไม่มีข้อมูล ให้เช็คทุกครั้งว่าในก้ามปู API มี `async (req, res) => { ... }`
> - **ไม่กันพัง (ลืม Catch Error):** สมมติ API ต้องดึงข้อมูลผู้ใช้ แต่ Database ล่มจู่ๆ หรือลบคอลัมน์ทิ้ง ถ้าเราไม่มี `try { ... } catch (error) { ... }` ครอบไว้ เซิร์ฟเวอร์ Express จะขึ้นขีดแดง **"Crash" ดับคามือ** และต้องสั่งรันเซิร์ฟใหม่ (เว็บตายสนิท 💀)
